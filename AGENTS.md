# AI Agent Instructions (AGENTS.md)

このファイルは、18xxサマリーサイトを編集・管理するAIエージェント向けの指示書です。作業開始前に必ず確認してください。

## プロジェクト概要

- **目的**: 18xxボードゲームのルール要点を、経験者がプレイ中に検索しやすいWeb形式とテキスト形式で公開する
- **技術スタック**: MkDocs + Material for MkDocs
- **管理者**: 非エンジニアの個人
- **ルール内容の正本**: 作業モードにより「ユーザー提供Markdown」または「サマリー作成プロジェクトの公式資料・ソース台帳・ルール台帳」

## 二つの作業モード

依頼内容からモードを判定し、混同しないこと。不明な場合は既存ページを変更せず、どちらのモードとして扱うか確認する。

### モードA: ユーザー提供Markdownの掲載

ユーザーが完成済みのMarkdown原案を提供し、そのまま掲載する従来フロー。

- AIは日本語表現やルール内容を変更しない。
- Markdown構文、必須構造、タブ配置、リンクを検証する。
- 問題があれば具体的な行と修正方法を報告し、ユーザーの修正版を待つ。

### モードB: ルールリソースからWebサマリーを作成

公式ルール、公式エラッタ、ソース台帳、ルール台帳などから、CodexがWeb向けサマリーを新規作成または改訂するフロー。

- `.codex/skills/publish-18xx-summary-to-site/SKILL.md` を全文読み、その手順に従う。
- 完成PDFを再要約するのではなく、上流のソース台帳とルール台帳を正本として本文を構成する。
- AIは出典に基づく情報選択、構造化、Web向けの日本語草稿を行ってよい。
- ユーザーは版境界、未解決裁定、用語、初回差し替え内容を承認する。
- 既存ページを初めてモードBへ移行するときは、出典付き差分報告を提示し、ユーザーの明示的な承認を得てから本文を置き換える。

## 出典と権威

モードBでは次の優先順位を守る。

1. 現行の公式エラッタ・公式FAQ
2. 対象版の公式ルールブック
3. 公式ボード、チャーター、早見表、コンポーネント記載
4. 公式の出版社・デザイナー回答（出所を明示）
5. ユーザー承認済みの卓上裁定（公式規則と区別）
6. 従前の日本語サマリー、完成PDF、デジタル実装、非公式FAQ（比較・欠落発見のみ）

別ゲームの慣例やテンプレート内の例示値から、対象ゲームのルールを推測してはならない。版違いを無言で統合しない。

Web公開または実質的な改訂前に、Web上の公式エラッタを再確認し、確認日をマニフェストへ記録する。上流のソース台帳より新しい変更を発見した場合、サイトだけで解釈せず、上流リソースの再調整を先に行う。

## ファイル構造と責務

- `docs/games/` — ゲームのWebサマリーMarkdown
- `docs/games/index.md` — ゲーム一覧
- `docs/games/.pages` — ナビゲーション定義
- `docs/_template.md` — サイト構造のテンプレート。例示ルールは事実として再利用禁止
- `docs/assets/` — `scripts/export_text.py` が生成するテキスト版。手動編集禁止
- `source-manifests/` — 上流リソースとサイト本文の受け渡し記録
- `.codex/skills/publish-18xx-summary-to-site/` — モードBの再利用可能な手順と契約
- `scripts/validate_source_manifests.py` — マニフェスト構造と上流ファイルの検証
- `scripts/check_site.ps1` — 公開前の一括検証
- `docs/stylesheets/extra.css` — 印刷・モバイル基盤。変更禁止

## モードBの受け入れ条件

対象ゲームの `source-manifests/<ゲーム名>.json` を作成し、少なくとも次を上流リソースへ対応付ける。

- `source-inventory.md`
- 安定IDを持つルール台帳（例: `sr-or-rule-ledger.csv`）
- `action-owner-map.md`
- `rulebook-outline.md`
- `uncertainty-register.csv`

次の場合は公開本文を変更しない。

- 版または資料の権威が固定されていない
- 必須リソースが欠けている
- 競売、送金主体、終局時点など高リスク規則が台帳化されていない
- 公式資料間の矛盾が未解決
- プレイ結果を変える曖昧さについて、明示的な掲載方法またはユーザー裁定がない
- マニフェストの `readiness.status` が `blocked`

`conditional` は無条件の公開許可ではない。未解決事項が掲載範囲外であるか、公式未規定として明示できる場合だけ草稿を進める。

## Webサマリーの内容契約

- 対象読者は18xx経験者。一般的なルールの再説明より、作品固有の相違、数値、条件、例外、状態遷移を優先する。
- 行動を先に、条件を次に、例外を最後に書く。
- プレイヤー、会社、銀行、システムなど、実際の行為主体と支払先・受取先を明記する。
- 株価移動は盤面上の方向が操作方法なら `左・右・上・下` を用いる。
- タイトル固有ルールを意図的に省略するときは、マニフェストへルールIDと省略理由を記録する。
- 数値、トリガー、例外は安定したルールIDへ追跡できなければならない。
- 同じ規則を複数タブへ全文重複させず、主たる使用箇所に置いて短い相互参照を使う。

### タブの役割

- `SR`: 初期競売・請願、株式売買、会社設立・成立、社長交代、保有制限、優先権、SR終了処理
- `OR`: 運営順、線路、駅、経路、収益、配当、株価変動、列車、緊急資金、会社金融、OR終了処理
- `セットアップ / 早見`: 準備、フェイズ、列車、会社・私鉄、上限、ボーナス、反復参照表
- 合併、国有化、中間ラウンドなど独立した進行は必要に応じて追加タブを使う

印刷用サマリーの4ページ順を、そのままWebのタブ順にしない。プレイ中の検索位置を優先する。

## 必須構造と技術検証

すべてのゲームページで次を確認する。

- `# <ゲーム名> サマリー` のタイトル
- `<div class="actions">` ブロック
- `print_button()` と正しいテキストDLリンク
- `=== "SR"` と `=== "OR"`
- `=== "セットアップ / 早見"`、または `=== "会社の種類 / 準備"` と `=== "早見表"`
- HTMLタグ、テーブル、リスト、コードブロックの構文
- タブと見出し階層、相対リンク

1862の既存 `その他` タブは移行まで互換扱いとするが、新規作成では標準タブ名を使う。

## 作業手順

### 既存ページをモードBへ移行

1. マニフェストと上流リソースを検証する。
2. 公式エラッタの鮮度を確認する。
3. 現行サイト本文とルール台帳を比較する。
4. 各差分を「維持・訂正・追加・削除・ユーザー判断」に分類する。
5. マニフェストにタブ別の採用ID・省略ID・未解決Issue IDを記録する。
6. 差分報告をユーザーへ提示し、初回差し替えの承認を得る。
7. 承認後に `docs/games/<ゲーム名>.md` を更新する。
8. `scripts/export_text.py` と `scripts/check_site.ps1` を実行する。
9. マニフェストの比較日・公開状態を更新し、変更ファイルを報告する。

### モードBで新規ゲームを追加

1. 上記の受け入れ条件と内容契約を満たす。
2. `docs/games/<ゲーム名>.md` を作成する。
3. `docs/games/index.md` と `docs/games/.pages` を適切な順序で更新する。
4. テキスト版を生成し、全検証を実行する。
5. `.md`、`.txt`、マニフェスト、一覧、`.pages` を提示する。

### ユーザー提供Markdownを掲載

1. 内容を変更せずMarkdown構文と必須構造を検証する。
2. 既存更新なら対象 `.md` を保存する。新規なら一覧と `.pages` も更新する。
3. テキスト版を生成し、全検証を実行する。
4. 技術的不整合がある場合は、行番号と修正方法を提示して修正版を待つ。

## 検証コマンド

通常の公開前検証:

```powershell
.\scripts\check_site.ps1
```

上流リソースの実在も含むモードB検証:

```powershell
.\.venv\Scripts\python.exe scripts\validate_source_manifests.py --source-root "<サマリー作成リソースのルート>"
```

Markdown更新時は必ず `scripts/export_text.py` を実行する。`docs/assets/` は直接編集しない。

## 変更禁止・要確認

### 変更禁止

- `docs/stylesheets/extra.css`
- `.github/workflows/` 配下
- `requirements.txt` の既存パッケージ削除
- `docs/assets/` の手動編集

### 事前確認が必要

- `mkdocs.yml` のテーマ・プラグイン設定
- 新規ゲーム追加以外の `docs/games/.pages` の構造変更
- `scripts/export_text.py` の生成ロジック変更
- サイト全体のデザイン変更
- 上流のサマリー作成プロジェクトへの書き込み

## 結果報告

- 使用した版と公式資料の最終確認日
- マニフェストのreadinessと未解決事項
- 採用・省略・保留したルールID
- 更新したMarkdownと生成したテキスト版
- 一覧・ナビゲーション・マニフェストの変更
- 実行した検証と結果
- ユーザー判断が必要な内容

ルールの正確性を断定するときは必ず出典へ戻れる状態にする。サイト本文を上流台帳と別系統の正本にしない。
